public class Primo {
   private int num=0;
    public Primo(){
     num=0;
    }
    public boolean getPrimo(num){
        this.num = num;
        boolean sim= true;  // se for primo
        sim = num%2==0? false : true;// não é primo
        if (!sim){
            return = false;
        }
        sim = num%3==0? false : true;// não é primo
        if (!sim){
            return = false;
        }
        sim = num%4==0? false : true;// não é primo
        if (!sim){
            return = false;
        }
        sim = num%5==0? false : true;// não é primo
        if (!sim){
            return = false;
        }
        }
    }
}
